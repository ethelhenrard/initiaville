{% extends 'base.html.twig' %}

{% block title %}
    {{ parent () }}
    - Accueil
{% endblock %}

{% block header %}
    {% include 'default/_menu.html.twig'  %}
{% endblock %}

{% block body %}


        <div class="jumbotron">
            <h1 class="display-4">InitiaVille</h1>
            <p class="lead">Proposez des initiatives pour améliorer la vie en ville.</p>
            <a class="btn btn-primary btn-lg" href="{{path('city_index')}}" role="button">
                <i class="fa fa-map-marker"></i>
                Voir les villes
            </a>
            <a class="btn btn-primary btn-lg" href="{{ path('project_new') }}" role="button">
                <i class="fa fa-plus-circle"></i>
                Proposer un nouveau projet
                {#todo restreindre l'accès > si pas user > envoyer vers page de log in -  rajouter un message? ou rajouter bouton pour nouveau user depuis log in?#}
            </a>
        </div>

        <h2>Les derniers projets</h2>
            <div class="row row-cols-1 row-cols-md-3">
            {% for project in projects %}
                <div class="col mb-4">
                {% include 'project/_card.html.twig' with {'project': project} only %}
                </div>
            {% endfor %}
            </div>

{% endblock %}


