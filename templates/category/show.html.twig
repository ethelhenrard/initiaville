{% extends 'base.html.twig' %}

{% block title %} {{ parent () }} - Projets {{ category.label }}{% endblock %}

{% block header %}
    {% include 'default/_menu.html.twig'  %}
{% endblock %}

{% block body %}

    <h2>
        {% if ((category.projects|length) == 0) %}
            Pas de projet en cours
        {% elseif (( category.projects|length) ==1) %}
            Projet
        {% else %}
            Projets
        {% endif %}
            dans la catégorie {{ category.label }}
    </h2>
    <div class="row row-cols-1 row-cols-md-3">
        {% for project in category.projects %}
            <div class="col mb-4">
            {% include 'project/_card.html.twig' with {'project': project} only %}
            </div>
        {% endfor %}
    </div>

{% endblock %}